{
  "name": "clawshi-arena",
  "version": "1.0.0",
  "description": "Multi-model agent arena for Clawshi prediction markets",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "agent:a": "node index.js --agent=claude-sentinel",
    "register": "node scripts/register-agents.js",
    "test": "node index.js --dry-run --agent=claude-sentinel --once"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  }
}
